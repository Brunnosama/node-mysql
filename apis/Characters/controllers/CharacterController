class CharactersController {
    //static significa que é uum método estático, não precisa criar uma nova isntancia
    static async getAll(req, res) {
        return res.send("ClassController");
    }
    static soma(a, b) {
        let soma = a + b;
        return soma;
    }
};

module.exports = CharactersController;